services:
  web:
    build: ./flask-app
    ports:
      - "5001:5000"
    volumes:
      - ./flask-app:/app
      - todo-data:/app/data
    environment:
      - FLASK_ENV=development
    restart: unless-stopped

volumes:
  todo-data:
